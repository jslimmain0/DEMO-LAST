# dev 프로파일: Oracle. SPRING_PROFILES_ACTIVE=dev FLOWLINK_DB_URL=... bash scripts/start.sh
# 사내/별도 서버 Oracle 로는 FLOWLINK_DB_URL 만 그쪽으로 바꾸면 된다(스키마는 src/main/resources/db/init.sql 을 그 DB 에 한 번 실행).
spring:
  datasource:
    url: ${FLOWLINK_DB_URL:jdbc:oracle:thin:@//localhost:1521/FREEPDB1}
    username: ${FLOWLINK_DB_USER:flowlink}
    password: ${FLOWLINK_DB_PASSWORD:flowlink}
