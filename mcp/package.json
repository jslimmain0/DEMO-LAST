{
  "name": "flowlink-mcp",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "FlowLink MCP 서버(Streamable HTTP) — FlowLink 옆에 떠서 URL 한 줄로 붙고, 로그인은 브라우저 GitHub(OAuth). 에이전트가 프로토콜·Mock·워크플로를 만들고 실행·관찰한다.",
  "scripts": {
    "start": "node src/index.js",
    "test": "node test/smoke.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "zod": "^4.6.5"
  }
}
