{
  "name": "flowlink-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FlowLink MCP server (stdio) — 에이전트가 프로토콜·Mock·워크플로를 만들고 실행·관찰한다. 실행: node src/index.ts (Node 22.6+ 타입스트리핑, 빌드 없음)",
  "scripts": {
    "start": "node src/index.ts",
    "test": "node test/smoke.ts"
  },
  "engines": {
    "node": ">=22.6"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.6.5"
  }
}
